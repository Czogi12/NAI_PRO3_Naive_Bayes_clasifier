import interfaces.ISmoothing;

public class NaiveBayesClassifier {
    private final ISmoothing smoothing;

    public NaiveBayesClassifier(ISmoothing smoothing) {
        this.smoothing = smoothing;
    }

//    public void applySmoothingAll()
}
